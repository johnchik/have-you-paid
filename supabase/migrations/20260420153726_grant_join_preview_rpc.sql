-- No-op placeholder: `get_session_join_preview` and `GRANT EXECUTE` are defined in
-- `20260419120000_guest_limits_kick_join_preview.sql`. This file keeps migration
-- ordering/history without duplicating those statements.
select 1;
